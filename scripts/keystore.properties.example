# Copia a android/keystore.properties (no versionar) tras generar el keystore.
# npm run keystore:generate crea android/eligr-upload.keystore por defecto.

storeFile=eligr-upload.keystore
storePassword=TU_STORE_PASSWORD
keyAlias=eligr-upload
keyPassword=TU_KEY_PASSWORD
